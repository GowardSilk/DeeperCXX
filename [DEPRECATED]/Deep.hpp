#ifndef DEEP_HPP
#define DEEP_HPP

#include "DeepMain.hpp"

namespace DEEP {
	class Mainframe {
		protected:
			//destructor
			virtual ~Mainframe() = default;
			//virtual functions
			template<typename T>
			void hijack(T f) {
				
			}
			void override() {

			}
		private:		
	};
} //!namespace DEEP

#endif