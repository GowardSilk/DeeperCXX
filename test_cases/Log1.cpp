#include "../DEEP_EYE.hpp"
#include <random>

int main() {

    std::ofstream wf("Log1.dat", std::ios::out | std::ios::binary);
    if(!wf) {
        std::cout << "cannot open file!\n";
        exit(EXIT_FAILURE);
    }

    

    return DEEPER;
}